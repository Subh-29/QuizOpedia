# Replace <username>, <password>, <host>, <port>, and <database_name> accordingly
# For local dev, example:
# DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/quiz_app"


# PostgreSQL Database connection string
DATABASE_URL="postgresql://<username>:<password>@<host>:<port>/<database_name>"
PORT=10000 #OR ANY PORT
# JWT secret for authentication
JWT_SECRET="your_jwt_secret_here"

# Gemini API key (for quiz generation or related AI features)
NEXT_PUBLIC_GEMINI_API_KEY="your_gemini_api_key_here"

